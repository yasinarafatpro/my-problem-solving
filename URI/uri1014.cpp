#include <bits/stdc++.h>
#define read() freopen("input.txt", "r", stdin)
#define write() freopen("output.txt", "w", stdout)
using namespace std;

int main(void)
{
    int n;
    cin >> n;
    double m;
    cin >> m;

    cout << fixed << setprecision(3) << n / m << " km/l" << endl;

    return 0;
}
