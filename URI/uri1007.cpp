#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    int a, b, c, d;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;

    cout << "DIFERENCA = " << (a * b - c * d) << endl;

    return 0;
}
