#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    double a, b, c;
    cin >> a >> b >> c;

    cout << fixed << setprecision(1) << "MEDIA = " << (a * 2.0 + b * 3.0 + c * 5.0) / 10.0 << endl;

    return 0;
}
